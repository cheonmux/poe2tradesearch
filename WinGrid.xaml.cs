using System.Windows;

namespace Poe2TradeSearch
{
    public partial class WinGrid : Window
    {
        public WinGrid(bool isQuad, int left, int top)
        {
            InitializeComponent();
        }
    }
}
